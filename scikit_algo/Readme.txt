All.py = many scikit algorithms with data preprocessing. use it by turning off/on different parts of script

All other python files are combined in All.py 

all-merged-cat.csv = dataset (old)
randomforest.png, svm.png = are the best score achieved by different combination 